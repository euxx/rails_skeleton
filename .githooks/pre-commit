#!/bin/sh
# Run RuboCop and Brakeman before committing.
# To skip: git commit --no-verify

set -e

# Git hooks run in a minimal shell; prepend mise shims so the correct Ruby is used
export PATH="$HOME/.local/share/mise/shims:$PATH"

bin/rubocop --format quiet
bin/brakeman --no-pager -q
